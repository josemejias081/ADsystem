<h1>Almacen Creado</h1>
<div class="card">
  <div class="card-header bg-info">
      <h3 class="card-title pt-2 text-white">Productos</h3>
    
  </div>
  <div class="card-body">
    <div class="table-responsive">
      <table class="table table-striped">
        <thead>
          <tr>
            <th>Cod</th>
            <th>Nombre</th>
            <th>Descripción</th>
            <th>Cantidad</th>
            <th>Actualizado</th>
          </tr>
        </thead>
        <tbody>
          <% @warehouse_record.warehouse_details.each do |warehouse_detail|%>
            <tr>
              <td> <%= warehouse_detail.product.id %></td>
              <td> <%= warehouse_detail.product.name %></td>
              <td><%= warehouse_detail.product.description %></td>
              <td><%= warehouse_detail.quantity %></td>
              <td><%= I18n.l(warehouse_detail.updated_at, format: '%d/%m/%Y: %H:%M') %></td>
              
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>
    <div class="card-footer">
      <div class="card-body">
        <%= link_to "Editar", edit_warehouse_record_path(@warehouse_record), class: "card-link" %> |
        <%= link_to "Eliminar", @nwarehouse_record, data: {turbo_method: :delete}, class: "card-link" %>
      </div>
    </div>
  </div>
</div>

